# Leave unset for local dev - the Vite dev server proxies /api to the local backend.
# Set to the deployed API's base URL for production builds, e.g. https://api.chaosgachaplus.example.com
VITE_API_BASE_URL=
