{
  "name": "chaos-gacha-plus",
  "private": true,
  "version": "0.1.0",
  "description": "A community-driven expansion of Bronzdeck's Chaos Gacha, rebuilt as a responsive web app.",
  "license": "GPL-3.0-only",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace apps/api",
    "dev:web": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace @chaosgachaplus/shared && npm run build --workspaces --if-present",
    "build:api": "npm run build --workspace @chaosgachaplus/api",
    "build:web": "npm run build --workspace @chaosgachaplus/web",
    "start:api": "npm run start --workspace @chaosgachaplus/api",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  }
}
